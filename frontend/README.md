# Laikipia County Job Portal — Frontend

React frontend for the Laikipia County Public Service Board recruitment portal.

## Setup

```bash
cd frontend
npm install
npm run dev
```

Backend must run at `http://localhost:8080` (configure via `.env`).

## PDF Reports

Staff views for applications, shortlists, final selection, and interviews render bulk lists as embedded PDF documents with download and print options.
